{
  "name": "otter-bank",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "back/**/*.rb": "bash -c 'cd back && /home/jinxin/.rbenv/shims/bundle exec rubocop -A --force-exclusion \"$@\"' --",
    "front/**/*.{ts,tsx,js,jsx}": "bash -c 'cd front && ./node_modules/.bin/eslint --fix \"$@\"' --"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
